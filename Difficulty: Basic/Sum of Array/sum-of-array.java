// User function Template for Java

class Solution {
    int arraySum(int arr[]) {
        int sum=0;
        for(int n:arr){
            sum+=n;//sum=sum+n;
        }
        return sum;
    }
}
/*
Approach
variable
loop

loop -> we will iterate numbers from array one by one
variable -> sum in which we will add each and every value from array
*/